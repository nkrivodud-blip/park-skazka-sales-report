"use client";
import { useState } from "react";
import { reportPeriods,type ReportPeriodKey } from "./reportData";
const money=(v:number)=>v?`${(v/1e6).toLocaleString("ru-RU",{minimumFractionDigits:2,maximumFractionDigits:2})} млн ₽`:"0 ₽";const avg=(s:number,c:number)=>c?money(s/c):"0 ₽";
export default function ManagerDashboard(){const[key,setKey]=useState<ReportPeriodKey>("september");const data=reportPeriods[key];return <section className="section" id="managers"><div className="period-toolbar reveal"><div><h2>Показатели по менеджерам</h2><span>{data.range}</span></div><label><span>Период</span><select value={key} onChange={e=>setKey(e.target.value as ReportPeriodKey)}>{Object.entries(reportPeriods).map(([k,v])=><option value={k} key={k}>{v.label}</option>)}</select></label></div>{(["B2C","B2B"] as const).map(direction=>{const rows=data.managers.filter(m=>m.direction===direction&&m.name!=="Наталья Криводуд"&&m.name!=="Александр Поленко"&&!(direction==="B2B"&&m.name==="Людмила Запорожец")).sort((a,b)=>Number(["Дмитрий Григорьев","Татьяна Баландина"].includes(a.name))-Number(["Дмитрий Григорьев","Татьяна Баландина"].includes(b.name)));return <div className="table-card manager-group reveal" key={direction}><div className="table-title"><div><h3>{direction}</h3><p>Конверсия, факт, предоплата и pipeline</p></div><span>План периода: {data.plan?money(data.plan[direction]):"0 ₽"}</span></div><div className="data-table manager-table"><div className="data-head"><span>Менеджер</span><span>Конверсия</span><span>Проведено</span><span>Предоплачено</span><span>Взвешенный pipeline</span><span>Сырой pipeline</span><span>План</span><span>Выполнение</span><span>Осталось</span></div>{rows.map(m=>{const plan="plan" in m?m.plan:0;const execution=plan?m.fact.sum/plan*100:0,conversion=m.total?(m.fact.count+m.prepaid.count)/m.total*100:0;return <div className="data-row" key={`${m.name}-${direction}`}><strong>{m.name}</strong><span><strong>{conversion.toLocaleString("ru-RU",{maximumFractionDigits:1})}%</strong><small>{m.fact.count+m.prepaid.count} из {m.total}</small></span>{(["fact","prepaid","weighted","raw"] as const).map(metric=><span key={metric}><strong>{key==="august"&&metric!=="fact"?"—":money(m[metric].sum)}</strong><small>{m[metric].count} шт. · чек {avg(m[metric].sum,m[metric].count)}</small></span>)}<span><strong>{money(plan)}</strong></span><span><strong>{plan?execution.toLocaleString("ru-RU",{maximumFractionDigits:1})+"%":"0%"}</strong>{plan>0&&<progress max={100} value={Math.min(100,execution)}/>}</span><span><strong>{money(Math.max(0,plan-m.fact.sum))}</strong></span></div>})}</div></div>})}<aside className="note reveal"><strong>Методика конверсии</strong><p>Конверсия = (успешные + предоплаченные сделки) ÷ все сделки менеджера в выбранном периоде. Пустой план считается нулевым. Выполнение рассчитывается только по факту.</p></aside></section>}
